server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/transport_db
    username: kinfe
    password: transport123

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB